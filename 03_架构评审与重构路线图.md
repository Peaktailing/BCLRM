# 架构评审与重构路线图（BCLRM）

> 原则：**增量、可回退、每步有测试保护**。不要一次性大重构——先止血（P0），再纠偏（P1），最后上工程化（P2）。
> 每一阶段结束都应让 CI 全绿、功能不变（回归测试通过）。

---

## 阶段 0：止血（P0，必须最先做，约 1–2 天）

目标：消除"悄悄出错"和"数据损坏"风险。

### 0.1 修复异常被静默吞掉（H1）
- **改动**：`db/base_service.py` 中所有 `except Exception: return []/None/0` 改为**重新抛出**（或返回显式失败结果），由上层（service/business/UI）统一处理错误展示。
- **验证**：新增一个测试——让查询故意失败，断言调用方收到异常而非空数据。
- **注意**：这会改变 `get_by_id` 返回 `None` 的语义（`None` 现在只能代表"查不到"），全仓搜一下调用点，统一处理。

### 0.2 修复 SQLite 连接线程安全（H2）
- **方案 A（推荐，最小改动）**：给 `Database` 加一把 `threading.Lock`，所有 `execute_*` 在锁内执行。简单够用，单写多读场景 OK。
- **方案 B（更稳）**：弃用全局单例，改为连接池 / 每请求短连接（Streamlit 每次 rerun 取连接、用完还）。改动大，留到阶段 2 评估。
- **验证**：写一个并发读写测试（多线程同时操作），断言无异常、数据一致。

### 0.3 清理仓库里的真实数据（M2）
- `git rm --cached db/reagent.db db_wal/ attachments/ logs/` 等运行时产物；在 `.gitignore` 补齐 `db_wal/`、`*.db*`、`*-wal`、`*-shm`、`attachments/`、`logs/`、`.uploads/`。
- 数据库只保留**建表/迁移脚本**（`init_tables` 已在代码里，足够），运行靠初始化生成空库。
- 顺手确认：当前代码用的是 `db/main.db`，`reagent.db` 是旧名残留，清理后无影响。

---

## 阶段 1：纠偏（P1，约 3–5 天）

目标：让"架构声明"与"实际行为"一致，消除反范式。

### 1.1 让 `updated_at` 真正更新（M3）
- 在 `init_tables` 末尾为每个核心表加 `AFTER UPDATE` 触发器：`SET NEW.updated_at = CURRENT_TIMESTAMP`；或在 service 层 UPDATE 时显式赋值。推荐触发器（DB 层保证，不被业务代码遗漏）。

### 1.2 引入跨表事务（M4）
- 在 `Database` 加 `transaction()` 上下文管理器：`BEGIN` → `commit()` / `rollback()`，内部 `execute_*` 不再各自 commit。
- 把"领用""归还"等涉及多表的用例用事务包裹。

### 1.3 状态字段现代化（M5）
- `borrowable_flag` 由 `'可借'/'不可借'` 文本 → 保留 `borrowable_check INTEGER`（布尔）即可，查询改用布尔；或引入 `ReagentStatus` 枚举 + 映射。删除冗余文本列。

### 1.4 `reagent_type` 改关联表（M6）
- 新建 `chemical_reagent_type(chemical_id, type_id)` 关联表，删除 `chemical_info.reagent_type` 的 CSV；提供查询/写入辅助方法。迁移脚本把现有 CSV 拆入关联表。

### 1.5 处理"独立 WAL 目录"的谎言（M1）
- 二选一：
  - **务实版**：承认 SQLite 单库无法把 WAL  relocate，删除 `wal_dir` 参数与 `db_wal/` 目录，文档如实说明 WAL 与库同目录。
  - **进阶版**：确实需要隔离，可用 `SQLITE_TMPDIR` 环境变量或在打开连接前 `os.environ` 设置，并写测试验证 WAL 文件落点。
- 无论哪种，**删掉"建了不用的空目录"这件事**。

---

## 阶段 2：工程化与质量门禁（P2，约 2–3 天，与上面可并行）

目标：把规范变成机器强制。

### 2.1 落地质量门禁（见 `configs/`）
- `pyproject.toml` 替代 `setup.py`+`requirements.txt`；锁版本（pip-tools / uv）。
- `.pre-commit-config.yaml` + `ci.yml` 接入 GitHub Actions，门禁全红即阻断合并。
- 先"只检查不阻断"跑一周，等告警清零再设为 required status check。

### 2.2 测试整理（L3）
- 把根目录 `test.py`（其实是 UI 页面）改名（如 `pages/0_环境自检.py`），`test_db_connection.py` 移入 `tests/`。
- 将 `tests/test_core.py` 按模块拆分，逐步迁移到 pytest 风格；加覆盖率门槛。

### 2.3 去除 `sys.path` 脏注入（L4）
- 依赖 `pip install -e .` 后正常 `import`，删光 `sys.path.insert(0, ...)`。

### 2.4 安全收尾（L5/L6）
- 认证失败统一文案，消除用户名枚举。
- 登录加失败次数限制（内存/DB 计数 + 锁死）。

---

## 阶段 3：团队能力固化（持续）

- **结对/评审节奏**：每周一次代码评审，用本文档的 PR 清单逐条勾选。
- **知识沉淀**：把本次发现的坑（吞异常、线程安全、WAL 谎言、CSV 反范式）写成 `docs/踩坑记录.md`，新人必读。
- **分支策略**：`main` 保护 + 功能分支 + PR 双审；CI 门槛卡死。
- **下一步可选演进**：评估 SQLAlchemy（减少手写 SQL）、引入 Alembic 做真正迁移管理、Streamlit 页面加缓存与权限粒度细化。

---

## 优先级速查

| 优先级 | 项 | 风险如果不做 |
|--------|----|--------------|
| 🔴 P0 | H1 异常吞掉 | 故障伪装成空白，排查无门 |
| 🔴 P0 | H2 线程安全 | 并发下数据损坏 |
| 🔴 P0 | M2 真实数据入仓 | 隐私泄露 + 仓库膨胀 |
| 🟠 P1 | M3/M4/M5/M6/M1 | 数据不一致、架构谎言、难维护 |
| 🟡 P2 | L1–L6 | 质量靠自觉，规范无法落地 |
