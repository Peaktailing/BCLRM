# BCLRM 代码质量评审报告

> 评审人：资深开发工程师（高级开发工程师）
> 评审对象：`Peaktailing/BCLRM`（生物/化学实验室试剂管理系统，Streamlit + SQLite + Python）
> 评审日期：2026-08-06
> 评审方式：基于 GitHub 仓库源码静态走查（app.py / db/* / services/* / components/* / tests/* / 配置与依赖）

---

## 一、总体评价

**结论：底子不错，但存在"正确性"和"架构诚实度"两类问题，需要尽快收敛。**

项目已经做了不少正确的事，说明团队有安全意识、也走过工程化尝试：

- ✅ SQL 注入防护：标识符（表名/字段名/排序）走白名单正则校验，值一律参数化（`?` 占位符）——这是教科书级做法。
- ✅ 密码安全：PBKDF2-HMAC-SHA256 哈希，明文不落库。
- ✅ 数据建模：11 个 Pydantic 模型替代裸 dict，且 Service 层已支持依赖注入便于测试。
- ✅ 测试基础：48 个单元测试，覆盖模型校验、密码工具、DB 单例、CRUD、权限、认证、入库。
- ✅ 日志脱敏：错误日志只记参数数量与类型，不记实际值（PII 防护）。
- ✅ `.gitignore` 已覆盖 `.env`、`secrets.toml`、`key.txt`、`logs/`。

但评审也发现 **2 个高优正确性缺陷** 和 **若干"架构声明与实际不符"的隐患**，这些是资深把关要重点解决的部分。

---

## 二、问题清单（按严重度排序）

### 🔴 高优（P0，影响正确性或数据安全）

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| H1 | **异常被静默吞掉，故障伪装成"无数据"** | `db/base_service.py` 所有 `get_*`/`create`/`update`/`delete` 方法 | 方法内部 `except Exception: return [] / None / 0`。底层 `Database` 抛错后，调用方拿到的是空列表或 `None`，**无法区分"查不到"与"数据库崩了"**。后果：DB 出错时界面静默显示空白库存，而非报错，极难排查。 |
| H2 | **SQLite 连接线程不安全（并发数据损坏风险）** | `db/database.py` `connection` 属性 `sqlite3.connect(..., check_same_thread=False)` + 全局单例 | 单例缓存一个连接并在 Streamlit 多会话/多线程间共享，但**没有任何锁**。`check_same_thread=False` 只是关掉检查，不提供并发安全。多用户同时操作时可能触发 `SQLite objects created in a thread...` 或更隐蔽的数据错乱。 |

### 🟠 中优（P1，架构诚实度 / 可维护性）

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| M1 | **"独立 WAL 目录"是空架子** | `db/database.py` `_configure_wal` 注释明说 SQLite 不支持 `PRAGMA journal_location`，且代码从未设置 `SQLITE_TMPDIR`；`multi_database.py` 传的 `wal_dir` 只被用来 `os.makedirs` 建了个**永远不会被 SQLite 写入的空目录** | 四库分离架构里"独立 WAL 目录"这一卖点实际未生效，且 `db_wal/` 被无谓地提交进仓库。 |
| M2 | **`db/reagent.db`（512KB 真实数据）被提交进 Git** | 仓库根 `db/reagent.db`；`settings.py` 实际用的是 `db/main.db` | `.gitignore` 虽写了 `db/*.db`，但该文件先提交后加忽略，导致一份真实（含人员/试剂）数据长期留在版本库，且新旧两个库并存造成混淆。`db_wal/`、`attachments/`、`logs/` 等运行时目录同理。 |
| M3 | **`updated_at` 永远不会更新** | `db/database.py` `init_tables` 中 `updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP` | `DEFAULT` 只在 INSERT 生效，代码与迁移均未在 UPDATE 时维护该字段，也无触发器。审计/追踪用的"更新时间"实际恒等于创建时间。 |
| M4 | **没有跨表事务** | `db/database.py` `execute_update` 每次执行即 `commit()` | 领用/归还等涉及多表（瓶状态 + 领用记录 + 日志）的业务操作无法原子提交，中途失败会留下**部分写入**（数据不一致）。 |
| M5 | **状态用中文 TEXT 存储** | `reagent_bottle.borrowable_flag = '可借'/'不可借'`（`reagent_bottle_service.get_borrowable` 据此查询） | 用中文字符串当状态枚举，易拼写错误、无法约束、查询低效、且中英 i18n 时直接崩。应改为布尔/枚举。 |
| M6 | **CSV 塞进单列（`reagent_type`）** | `db/database.py` `_migrate_chemical_info_drop_reagent_type_fk` 特意删外键以支持逗号分隔多类型 | 反范式设计，查询/校验困难，应改为"化学品-类型"关联表（junction table）。 |
| M7 | **模块级全局可变单例泛滥** | `db/database.py` `db = Database()`、`services/*` 各 `*_service` 全局实例 | 全局状态使测试必须 monkeypatch（见 `tests/test_core.py` 的 `_setup_service`），耦合高、并发下更难推理。 |

### 🟡 低优（P2，工程化/规范）

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| L1 | **没有 CI、没有 pre-commit、没有 ruff/mypy 配置** | 全仓库 | `.gitignore` 里有 `.ruff_cache/`、`.mypy_cache/`，但**没有任何对应配置或流水线**。质量门禁全靠人工自觉。 |
| L2 | **遗留构建配置分裂** | `setup.py` + `requirements.txt` | 依赖在两处重复声明且均为 `>=` 不锁版本，易漂移、不可复现。应统一到 `pyproject.toml` 并加 lock。 |
| L3 | **测试组织混乱** | 根目录 `test.py`（实为 Streamlit UI 页面）、`test_db_connection.py`；`tests/test_core.py` 单文件 20KB | `test.py` 命名会干扰 pytest 收集；测试应统一到 `tests/`，用 pytest 风格。 |
| L4 | **运行时 `sys.path.insert` 注入** | `app.py`、`tests/test_core.py` | 已有 `setup.py` 可安装包，仍靠 `sys.path` 脏注入，说明包结构未真正跑通。 |
| L5 | **认证存在用户名枚举** | `components/auth.py` 走 `person_service.authenticate`，返回码区分 `USER_NOT_FOUND`/`WRONG_PASSWORD` | 攻击者可借此探测哪些用户名存在（内部系统风险低，但属 CWE-203）。 |
| L6 | **登录无限流/锁死** | `components/auth.py` | 无失败次数限制，理论上可暴力破解（内部系统风险低）。 |

---

## 三、亮点（务必保留，别在重构时丢掉）

- SQL 标识符白名单校验 + 全程参数化（H1 之外，注入面已控制得很好）
- PBKDF2 密码哈希
- Pydantic 模型 + Service 依赖注入（已为测试打好基础）
- 四库分离的**设计意图**是对的（读写分离/审计隔离），只是 WAL 落地有偏差
- 日志脱敏实践
- 已有 48 个测试，重构有安全网

---

## 四、下一步

见《架构评审与重构路线图.md》——按 P0→P1→P2 分阶段、可增量执行。质量门禁配置（ruff/mypy/pytest/CI/pre-commit）见《Python 代码规范与质量门禁.md》及 `configs/` 目录下的即用文件。
